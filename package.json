{
  "name": "ts-mock",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build": "tsc -b",
    "test": "pnpm build && vitest run",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@types/node": "^22.15.30",
    "@vitest/coverage-v8": "3.2.2",
    "prettier": "^3.5.3",
    "typescript": "~5.8.3",
    "vitest": "^3.2.4"
  }
}
